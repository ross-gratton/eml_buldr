{% include "container.start.twig" with {
    module: "a1.2_header--text-logo",
    thumb: "a1.2_header--text-logo.png",
    outer_attributes: "data-bgcolor='a1.2_background--outer'",
    attributes: "data-bgcolor='a1.2_background--inner'",
    outer_class: "bg--daintree"
} %}
    {% include "content.start.twig" with { top: vertical_spacing, left: horizontal_spacing, right: horizontal_spacing } %}
        {% include "column.collection.start.twig" with { class: "mobile-stack mobile-center" } %}

            {# Column 1 #}
            {% include "column.start.twig" %} 
                {% include 'content.start.twig' with { class: "copy t-20 white", attributes: "data-color='a1.1_title-color'" } %}
                    Email Title
                {% include 'content.spacer.twig' with { class: "copy t-14 white", attributes: "data-color='a1.1_text-color'" } %}
                    January 1st 2018
                {% include 'content.end.twig' %}
            {% include "column.end.twig" with { gutter: 20 } %}

            {# Column 2 #}
            {% include "column.start.twig" with { attributes: "align='right'", class: "white" } %}

                {% include "image.twig" with { src: "200x50.png" } %}

            {% include "column.end.twig" %} 

        {% include "column.collection.end.twig" %}
    {% include "content.end.twig" with { bottom: vertical_spacing, left: horizontal_spacing, right: horizontal_spacing } %}
{% include "container.end.twig" %}  
